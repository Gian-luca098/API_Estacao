spring:
  application:
    name: estacao-meteorologica-api

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-us-west-1.pooler.supabase.com:6543/postgres?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:postgres.eyuggwjixeuraiqjbnze}
    password: ${SPRING_DATASOURCE_PASSWORD:estacao098!}
    driver-class-name: org.postgresql.Driver

app:
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:4200"
